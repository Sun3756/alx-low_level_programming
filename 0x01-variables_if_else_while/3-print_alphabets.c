#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)

{
	char x, y;

	for (x = 'a'; y = 'A'; 'a' <= 'z'; 'A' <= 'Z'; a++; A++)
	{
		putchar(x);
		putchar(y);
	}
	{
		putchar('\n')
	}
	return (0);
}

